@model WizardsRUs.Models.ACSRFThing
@{
    ViewBag.Title = "Step1";
}

<h2>Step1</h2>
@using (Html.BeginForm("step2", "acsrfwizard", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    @Html.LabelFor(m => m.Text)
    @Html.TextBoxFor(m => m.Text)
    <br />
    <input type="submit" />
}